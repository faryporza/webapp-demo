#!/bin/bash

# Navigate to the project directory
cd "$(dirname "$0")"

# Install face-api.js
npm install --save face-api.js

echo "✅ face-api.js has been installed."
echo "You can now use the face detection features."
